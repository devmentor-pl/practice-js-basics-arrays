const users = ["Adam", "Beata", "Staszek", "Kowalski", "Nowak"];

const info = `${users[0]} ${users[2]} ${users[users.length - 1]}`;
console.log(info);


const usersLength = users.length;
console.log(usersLength);